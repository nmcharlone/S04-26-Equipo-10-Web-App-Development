# Migraciones

Ejecutar el comando: _npm run migrate_, esto creara la base de datos en la raiz del proyecto con el script de migraciones, incluyendo una semilla para desarrollar pruebas. En produccion debe ser modificado de tal manera que solo estén disponibles los valores predeterminados de ls tablas que no se modifican en runtime.
